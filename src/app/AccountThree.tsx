import AccountContent from "../../src/app/AccountContent";

const accountNumber = 3;

export default function AccountThree(): JSX.Element {
  return <AccountContent accountNumber={accountNumber} />;
}
